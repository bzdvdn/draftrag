# Data Model: chromadb-collection-management

Эта фича не вводит новых сущностей, persisted state, state transitions или lifecycle rules.

Управление коллекцией — это операция над сервером ChromaDB (сторонняя система), а не изменение domain-модели draftRAG.
