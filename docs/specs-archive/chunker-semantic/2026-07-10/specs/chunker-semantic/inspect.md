---
report_type: inspect
slug: chunker-semantic
status: concerns
docs_language: ru
generated_at: 2026-07-10
---

# Inspect Report: chunker-semantic

## Scope

- snapshot: проверка spec семантического чанкера на полноту, однозначность, соответствие конституции и критериям приёмки
- artifacts:
  - .speckeep/constitution.summary.md
  - docs/specs/chunker-semantic/spec.md

## Verdict

- status: concerns — spec корректна, но есть минорные неоднозначности и одна расплывчатая формулировка в Evidence

## Errors

- none

## Warnings

1. **RQ-001: "базовая защита от сокращений" не специфицирована**
   - `spec.md:61` — не указано, какие сокращения обрабатываются и каким методом (список исключений? regex?).
   - Рекомендация: явно перечислить поддерживаемые исключения (Dr., Mr., Mrs., Ms., e.g., i.e., vs.) или сослаться на правило «только если после точки идёт пробел + строчная буква».

2. **AC-009 Evidence: "производит semantic-разбиение" — непроверяемо**
   - `spec.md:152` — фраза не содержит конкретного assertion. Рекомендация: заменить на `type assertion that Chunker is *semanticChunker` или `chunk count > 1 для документа с 2+ темами`.

3. **Открытые вопросы: `none` избыточен при наличии реальных вопросов**
   - `spec.md:179` — строка `- \`none\`` присутствует вместе с двумя вопросами выше. Рекомендация: удалить `none`.

4. **AC-005 Evidence — эвристика с заглавной буквы ненадёжна**
   - `spec.md:120` — проверка "начинается с заглавной буквы" не работает для предложений, начинающихся с числа, кавычки, имени собственного с маленькой буквы в начале и т.д. Рекомендация: усилить формулировку: "каждый chunk.Content заканчивается на `.`/`!`/`?`" (достаточно для контракта, что предложения не режутся).

## Questions

- none

## Suggestions

- Рассмотреть добавление AC для случая, когда Embedder возвращает ошибку (сейчас упомянуто только в Краевых случаях, но не как AC).
- Рассмотреть явное указание в spec, какой chunker используется по умолчанию при `chunker.type = "semantic"` без semantic-секции — ошибка или fallback на basic?

## Traceability

- 9 AC (AC-001–AC-009), все покрывают уникальные outcomes
- 9 RQ (RQ-001–RQ-009), каждое маппится на ≥ 1 AC
- План и задачи пока не созданы

## Next Step

- safe to continue to plan после исправления Warnings 1–3 (опционально)
